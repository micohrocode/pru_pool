interface RideRequest {
    user: string;
    pickup: string;
    drop_off: string;
    days: number;
    recurring: boolean;
    arrival_time: string;
}

const rideRequest: RideRequest[] = [

];

export default rideRequest